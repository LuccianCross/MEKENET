# Database
DATABASE_URL=postgresql://user:password@localhost:5432/mekenet

# API key the mobile app must send with every request
MEKENET_API_KEY=your-secret-api-key-here

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost
